git init
git add .
For remove from just tracking,
    git rm --cached frontend/v1
After commit,
    git restore --staged frontend/v1

git commit -m "Initial commit"
git reset HEAD jenkins_notes.md
hudbh